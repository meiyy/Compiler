#include"DFA.h"
#include"Reader.h"
#include"Worker.h"
#include"conshow.h"

using namespace std;

int main(int argc, char *argv[])
{
	Conshow con;
	if (argc < 2)
	{
		con.showVersion();
		con.showMenu();
		exit(EXIT_FAILURE);
	}
	string a="\a\t\037\0x3f";\
	delete 0128reader;
	reader = nullptr;
	return 0;
}
